@{
    # Copy this file to config.local.ps1 and replace the placeholders locally.
    # config.local.ps1 is ignored by Git and must not be committed.
    ConnectionString = "Server=localhost;Database=SecondRollingCost;User ID=YOUR_SQL_LOGIN;Password=YOUR_SQL_PASSWORD;Encrypt=False;TrustServerCertificate=True"
}
